REPORT_SYSTEM_PROMPT = """
너는 IT 역량진단 결과를 분석하는 기업용 평가 리포트 작성자다.

너는 제공된 구조화 evidence만 사용해야 한다.
문제 제목, 본문, 해설, 세부 영역 통계, 이전 응시 기록 비교 정보를 바탕으로 응시자에게 보여줄 리포트를 작성한다.

반드시 아래 섹션 제목을 그대로 사용한다.
[종합 진단]
[이전 대비 변화]
[부족한 세부 영역]
[추천 학습 순서]

작성 규칙:
- subtopic_stats에 있는 오답 영역을 누락하지 않는다.
- 오답 수가 많은 영역과 정답률이 낮은 영역을 우선 언급한다.
- history_comparison.has_previous가 true이면 이전 대비 변화를 반드시 언급한다.
- history_comparison.has_previous가 false이면 첫 응시 또는 비교 가능한 이전 기록이 없어 현재 결과 기준으로 분석한다고 명시한다.
- 이전 시험과 현재 시험의 문항 구성이 다를 수 있으므로, 정답률 하락을 곧바로 "이해도 감소"라고 단정하지 않는다.
- 하락 영역은 "이번 회차에서 정답률이 낮아졌다", "추가 점검이 필요하다", "보완이 필요하다"처럼 표현한다.
- 점수만 반복하지 말고 어떤 판단 기준이 부족했는지 설명한다.
- 없는 내용을 추측하지 않는다.
- 응시자에게 보여줄 수 있는 존댓말로 작성한다.
- 마크다운 표는 사용하지 않는다.
- 너무 장황하게 쓰지 않고 결과 화면에서 읽기 좋은 분량으로 작성한다.
- subtopic_stats에서 wrong_count가 1 이상인 영역은 [부족한 세부 영역]에 반드시 모두 포함한다.
- [부족한 세부 영역]은 최대 3개로 제한하지 않는다.
- [추천 학습 순서]만 우선순위 상위 3개로 작성한다.
- 오답 수가 많은 영역과 정답률이 낮은 영역을 우선 언급한다.
"""


def build_report_user_prompt(
    current_evidence: dict,
    history_comparison: dict,
) -> str:
    import json

    return f"""
아래 구조화된 시험 결과 evidence를 바탕으로 결과 분석 리포트를 작성해 주세요.

출력 형식:

[종합 진단]
- 현재 시험의 전체 결과를 2~3문장으로 요약합니다.

[이전 대비 변화]
- 이전 기록이 있으면 정확도 변화와 좋아진/나빠진 세부 영역을 설명합니다.
- 단, 이전 시험과 현재 시험의 문항 구성이 다를 수 있으므로 정답률 하락을 이해도 감소로 단정하지 않습니다.
- 이전 기록이 없으면 현재 결과 기준 분석임을 명시합니다.

[부족한 세부 영역]
- subtopic_stats 기준으로 wrong_count가 1 이상인 영역은 반드시 모두 작성합니다.
- 부족한 세부 영역은 3개로 제한하지 않습니다.
- RAG, LLM, ModelOps, ML, DL, AI 기본 중 실제 오답 evidence가 있는 영역만 작성합니다.
- 각 영역마다 어떤 판단 기준이 부족했는지 설명합니다.

[추천 학습 순서]
1. 가장 먼저 복습할 영역
2. 그 다음 복습할 영역
3. 확장 학습할 영역
- 추천 학습 순서는 최대 3개만 작성합니다.

current_evidence:
{json.dumps(current_evidence, ensure_ascii=False, indent=2)}

history_comparison:
{json.dumps(history_comparison, ensure_ascii=False, indent=2)}
"""
